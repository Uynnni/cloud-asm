<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title> Admin Page </title>
    <script type = "text/JavaScript">
      function AutoRefresh( t ) {
         setTimeout("location.reload(true);", t);
      }
</script>
    <style>
         *{
        padding: 0;
        margin: 0;
        box-sizing: border-box;
      }
  #header_message {
  display: flex;
  justify-content: space-between;
  align-items: center;
  width: 100%;
  height: 6vh;
  background: rgba(255, 255, 255, 0.2);
  backdrop-filter: blur(10px);
  border: 1px solid rgba(255, 255, 255, 0.18);
  box-shadow: 0 8px 32px 0 rgba(31, 38, 135, 0.37);
  color: #fff;
  font-size: 1.2rem;
  padding: 0 1rem;
}
.header_logo {
  color: #fff;
  font-size: 1.5rem;
  font-weight: 700;
  padding: 0 1rem;
}
.header_welcome {
  color: #fff;
  font-size: 1rem;
  font-weight: 500;
}
.header_logout {
  color: #fff;
  font-size: 1rem;
  font-weight: 500;
  padding: 0 1rem;
  cursor: pointer;
}
.header_logout:hover {
  color: black;
}

 body {
  font-family: 'Open Sans', sans-serif;
  font-weight: 300;
  line-height: 1.42em;
  color:black;
  background-color: #6667AB;
  background-size: cover;
}

h1 {
  font-size:2.5em; 
  font-weight: 300;
  text-align: center;
  color: #fff;
  margin-bottom: 40px;
}

h2 a {
  font-weight: 700;
  text-transform: uppercase;
  color: #FB667A;
  text-decoration: none;
}

.blue { color: #185875; }
.yellow { color: #FFF842; }

table{
    text-align: left;
    overflow: hidden;
    width: 80%;
    margin: 0 auto;
    display: table;
    padding: 0 0 8em 0;
}
th h1 {
	  font-weight: bold;
	  font-size: 1em;
  text-align: center;
  color: #fff;
}
 td {
	  font-weight: normal;
	  font-size: 1em;
	  box-shadow: 0 2px 2px -2px #0E1119;
}

td, th {
  padding: 10px 12px 10px 12px;
}

tr:nth-child(odd) {
	  background-color: #94ABD6;
}

tr:nth-child(even) {
	  background-color: #94ABD6;
}

th {
	  background-color: #1F2739;
}

td:first-child { color: black; }

tr:hover {
	        box-shadow: 0 6px 6px -6px #0E1119;
}

button[type="submit"],
input[type="submit"]{
  background-color: transparent;
    border: none;
    font-size: 15px;
    color: #fff;
}
button[type="submit"]:hover
input[type="submit"]:hover{
  cursor: pointer;
}
input[type="text"] {
    /* background-color: transparent; */
    border: 1px solid #fff;
    font-size: 15px;
    /* color: #fff; */
    /* width: 100%;
    height: 5vh; */
}
#header_message {
  display: flex;
  justify-content: space-between;
  align-items: center;
  width: 100%;
  height: 6vh;
  background: rgba(255, 255, 255, 0.2);
  backdrop-filter: blur(10px);
  border: 1px solid rgba(255, 255, 255, 0.18);
  box-shadow: 0 8px 32px 0 rgba(31, 38, 135, 0.37);
  color: #fff;
  font-size: 1.2rem;
  padding: 0 1rem;
}
.header_logo {
  color: #fff;
  font-size: 1.5rem;
  font-weight: 700;
  padding: 0 1rem;
}
.header_welcome {
  color: #fff;
  font-size: 1rem;
  font-weight: 500;
}
.header_logout {
  color: #fff;
  font-size: 1rem;
  font-weight: 500;
  padding: 0 1rem;
  cursor: pointer;
}
.header_logout:hover {
  color: green;
}
.selectBox{
    display: flex;
}
    </style>
</head>
<body  onload = "JavaScript:AutoRefresh(10000);">
    <div id="header_message">
        <h2 class="header_logo">ATN Shop</h2>
        <p class="header_welcome">Welcome <%= name %> </p>
        <!-- <h2 class="header_logout">Logout</h2> -->
        <a href="/logout">Logout</a>
      </div>
    <div class="selectBox">
            <%- selectBox %>
        </select>
     </div>
     <h1>The List Of Products</h1>
     <div> <%- result %> </div>
</body>
</html>